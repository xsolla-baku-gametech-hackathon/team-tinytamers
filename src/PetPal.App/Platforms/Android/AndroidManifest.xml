<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round"
                 android:supportsRtl="true" android:networkSecurityConfig="@xml/network_security_config"></application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- "Real Life Connect" axını üçün: uşaq tapdığı şeyin şəklini çəkir. Könüllüdür. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Valideyn bölməsinin qapısı: barmaq izi / üz PIN-in QISA YOLUdur.
         İcazə verilməsə də app işləyir — qapı sadəcə PIN soruşur. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
